<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= post.title %></title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <header>
      <h1>My Blog</h1>
      <nav>
        <a href="/">Home</a>
      </nav>
    </header>

    <main>
      <article>
        <% if (post.image) { %>
        <img src="<%= post.image %>" alt="<%= post.title %>" />
        <% } %>
        <h2><%= post.title %></h2>
        <p><%= post.content %></p>
        <p><em>Author: <%= post.userId.name %></em></p>
        <!-- Kullanıcı adını göster -->
        <p><em>Category: <%= post.categoryId.name %></em></p>
        <!-- Kategori adını göster -->
        <p>
          <small
            >Published on: <%= new Date(post.createdAt).toDateString() %></small
          >
        </p>
        <p><strong>Likes: <%= post.likes %></strong></p>
        <p><strong>Visitors: <%= post.countOfVisitors %></strong></p>
      </article>
    </main>

    <footer>
      <p>&copy; 2024 My Blog</p>
    </footer>
  </body>
</html>
