const express = require('express'); const mongoose = require('mongoose'); const
path = require('path'); const app = express(); const postRoutes =
require('./routes/postRoutes'); // MongoDB bağlantısı
mongoose.connect('mongodb://localhost:27017/blogdb', { useNewUrlParser: true,
useUnifiedTopology: true }); // EJS kullanımı app.set('view engine', 'ejs');
app.set('views', path.join(__dirname, 'views')); // Statik dosyalar
app.use(express.static(path.join(__dirname, 'public'))); // Yönlendirmeleri
ekleme app.use('/api', postRoutes); // Sunucuyu başlatma app.listen(3000, () =>
{ console.log('Server running on port 3000'); });
